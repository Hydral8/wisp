[project]
name = "wisp-proxy"
version = "0.3.0"
description = "Stateless MCP tool execution proxy"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=0.21.0",
    "mcp>=1.26.0",
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "httpx>=0.27.0",
]
